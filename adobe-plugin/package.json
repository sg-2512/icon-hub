{
  "name": "iconsearch-adobe-express",
  "version": "0.1.0",
  "private": true,
  "description": "IconSearch Add-on for Adobe Express",
  "scripts": {
    "start": "powershell -Command \"$env:PATH = 'C:\\Program Files\\Git\\usr\\bin;' + $env:PATH; npx @adobe/ccweb-add-on-scripts start --hostname localhost\"",
    "check": "node --check src/index.js"
  }
}
